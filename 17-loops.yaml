- name: install packges in loop methed
  hosts: web
  become: yes
  tasks:
  - name: install packges
    ansible.builtin.package:
     name: "{{item}}"
     state: absent
    loop:
     - nginx
     - mysql
     - postfix
     - httpd
     - redis  