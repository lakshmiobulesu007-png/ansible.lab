- name: enter the value and print the value
  hosts: web
  vars_prompt:
    - name: COURSES
      prompt: enter the COURSES NAME
      private: false
    - name: DURATION
      prompt: enter the DURATION
      private: false
    - name: TRAINER
      prompt: enter the TRAINER NEME
      private: false
  tasks:
    - name: print the tasks
      ansible.builtin.debug:
        msg: "hi i am a larning {{COURSES}}, DURATION:{{DURATION}}, trainer:{{TRAINER}}"
